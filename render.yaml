services:
  - type: web
    name: stripe-auth-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT app:app --workers 4 --threads 8 --timeout 120
    plan: free
    autoDeploy: true
